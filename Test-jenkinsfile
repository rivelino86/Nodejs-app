pipeline{
    agent any
    stages{
      stage('Dep install'){
       agent { docker { image 'node:14'}}
        steps{
            sh 'cat /etc/os-release'
            sh 'exit'
            sh 'apt update'
            sh 'apt install sudo -y'
            sh 'npm install'
           sh 'sudo chown -R 995:993 "/.npm'
        }
      }  
    }
}